import 'dotenv/config';
// not utilized yet
export default {
  expo: {
    name: "project-kisan",
    slug: "project-kisan",
    scheme: "kisan",
    extra: {
      backendUrl: process.env,
    }
  }
}
